[project]
name = "meigma-packages-docs"
version = "0.0.0"
description = "Local documentation for Meigma package repository automation."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.0",
]

[tool.uv]
package = false
