---
id: 007
title: New work session
started: 2026-07-21
---

## 2026-07-21 17:35 — Kickoff
Goal for the session: Start a fresh journal session; the substantive work request has not yet been provided.
Current state of the world: Phase 4 staging publication is complete on `main`, and the repository is ready for the next focused increment.
Plan: Bind this session to the current task, then wait for the user's actual request and proceed iteratively.

## 2026-07-21 17:46 — First-release DEB/RPM gap analysis
Goal: Determine what `meigma/incus-gh-runner` and `meigma/packages` need before the first `v1.0.0` release can ship installable DEB/RPM packages.
Current evidence: `incus-gh-runner` is clean on public `master` at `0b7a4b6`; Release Please PR #24 is open and green at `ed3ff0f`, and no tag or release exists. GoReleaser currently emits four raw binaries, four SBOMs, and one checksum file; Melange APKs feed only the OCI image. The release stager is hard-coded to those nine assets.
Findings: Add GoReleaser/nFPM DEB/RPM outputs for Linux amd64/arm64, include the safe systemd deployment surface without enabling the unconfigured service, stage and checksum all four packages, and prove native DEB/RPM installation in both architectures in the dry-run and tag workflows before merging the release PR. If canonical APT/DNF installation through `pkgs.meigma.dev` is required, `meigma/packages` also needs GitHub Release discovery, a real `projects.yml`, multi-architecture ingestion/index generation, a production-safe R2 root apply mode and protected environment, consumer dispatch, and post-publish smoke/recovery. The current publisher accepts exactly one DEB and one RPM and generates only one host-architecture APT index; only the `_staging/` environment exists.
Next: Present the work as small proof-driven slices and keep PR #24 unmerged until the package asset rehearsal passes.

## 2026-07-21 18:38 — Four release packages landed and rehearsed
Implementation: Opened `meigma/incus-gh-runner` PR #46 from `feat/release-packages` at `f73b86a`. GoReleaser/nFPM now builds DEB and RPM packages for Linux amd64 and arm64, including the binary, hardened systemd unit, tmpfiles policy, editable configuration, credential drop-in examples, licenses, and guarded daemon-reload scripts. The service remains disabled and unstarted after installation. Package assets are now staged with the raw binaries and SBOMs, covered by checksum/provenance validation, and documented as the preferred host installation path.
Validation: Pinned GoReleaser 2.17.0 produced all four packages locally. Clean ARM64 installations passed on Debian 13 and Fedora 44. The full local `root:check` gate and `actionlint` passed. Hosted Release Dry Run #29883466029 passed at the exact PR head, including clean DEB and RPM installs on native amd64 and arm64 runners; CI, CodeQL, GitHub Pages, and external repository checks are green. Release Please PR #24 remains unmerged.
Next: Review and merge PR #46 before allowing the first Release Please release PR to merge. Follow-on repository publication through `pkgs.meigma.dev` remains a separate slice.

## 2026-07-21 18:49 — Release package PR merged
Outcome: After maintainer approval, verified PR #46 was still at reviewed head `f73b86a`, its required CI and exact-head hosted package rehearsal were green, and it was mergeable. Squash-merged it on GitHub as `e69fd1c`, fast-forwarded the local `incus-gh-runner` `master`, and removed the clean integrated feature worktree and local/remote branch. Post-merge CI, both CodeQL runs, and the documentation build passed on `e69fd1c`. Release Please PR #24 remains open and was refreshed automatically by Release Please; it was not merged.
Next: The four GitHub Release package assets are ready for the first release. The next Phase 5 slice can freeze the real asset contract and implement `meigma/packages` release discovery and registry onboarding.

## 2026-07-21 19:16 — Published v1.0.0 verified
Outcome: Verified the public, non-prerelease `incus-gh-runner` `v1.0.0` release and tag both resolve to release commit `2ad80f9`; tag-triggered Release run #29884714489 succeeded on that exact SHA. All 13 expected assets exist. All 12 binary/package/SBOM files match `checksums.txt`, SPDX SBOMs parse, the native macOS ARM64 binary reports `1.0.0`, and GitHub SLSA provenance verifies against `.github/workflows/attest.yml` with `refs/tags/v1.0.0` and GitHub-hosted runners. Published DEB/RPM metadata is exactly amd64/arm64 and x86_64/aarch64; clean downloaded ARM64 installs passed on Debian 13 and Fedora 44 without enabling the service, while the tag workflow independently passed all four native installs. OCI digest `sha256:4445f7285ad45914495f0b847a25ac553f4495cfa65ab4120c1a4131d397f726` contains linux/amd64 and linux/arm64; its keyless Cosign signature and SLSA provenance both verify.
Hardening gap: The GitHub release API reports `immutable=false`, so `gh release verify v1.0.0` has no release-level attestation. Artifact checksums, GitHub asset digests, exact-workflow build provenance, and OCI signing are healthy, but future source ingestion should enforce those controls and a follow-up should enable immutable releases plus public copy-paste verification instructions.
Next: Freeze the exact four real package names in the `meigma/packages` source contract and implement the first real GitHub Release discovery/registry-onboarding slice.

## 2026-07-21 20:08 — Real release source proof established
Implementation: On `feat/phase5-release-source`, added the canonical root `projects.yml` entry for `meigma/incus-gh-runner`, including package identity, exact checksum asset, versioned DEB/RPM patterns, amd64/arm64 package metadata mappings, retention, and the pinned attestation signer workflow. Added an atomic `fetch-release` path that accepts only registered stable published releases, validates the complete package set, checks GitHub API SHA-256 digests and sizes, and verifies each package's SLSA provenance before exposing the download. Extended deterministic rebuilds to validate and index both APT and RPM architectures while preserving the fixture-backed legacy path.
Proof: Unit and failure-path tests pass, including incomplete release sets, duplicate architecture mappings, digest mismatch, and no partial output after provenance failure. The live `v1.0.0` fetch selected the checksum file and four packages and passed all four pinned-workflow attestation checks. `root:phase5-source-proof` then ingested those exact public assets, produced `binary-amd64` and `binary-arm64` APT indexes plus `x86_64` and `aarch64` RPM trees, and completed clean native DEB and RPM installs from the newly signed local repositories.
Boundary: This slice remains read-only toward GitHub and R2. The live proof is opt-in, uses no publishing credentials, and does not add an automated privileged workflow, production R2 apply, consumer dispatch, or cache/rollback changes.
Next: Finish documentation and full repository validation, then open the Phase 5 source-onboarding PR for review.

## 2026-07-21 20:19 — Phase 5 source PR ready
Delivery: Committed `feat(releases): ingest verified GitHub packages` as `362935b` on `feat/phase5-release-source`, pushed it, and opened `meigma/packages` PR #10. The PR remains intentionally limited to the canonical source registry, verified release fetch, multi-architecture local rebuild, and opt-in real-release proof.
Validation: Formatting, lint, all Go tests, build, workflow/shell policy, strict documentation build, legacy Phase 1 and Phase 2 proofs, and the real `v1.0.0` Phase 5 proof passed. Hosted CI and Kusari Inspector both passed on exact PR head `362935b6cd623d3f11f79ccfa45d0474bc38fabc`.
Next: Review PR #10. After merge, the next separately reviewable slice is the production-safe R2 root publication boundary and its empty-prefix recovery rehearsal; consumer dispatch remains after publication is proven.

## 2026-07-21 20:14 — Production publication boundary proven locally
Merge: After approval, verified PR #10 at reviewed head `362935b`, all hosted checks green, and squash-merged it as `4af78a9`. Local `main` is fast-forwarded and clean; post-merge CI run #29887027933 passed on that exact commit.
Implementation: On `feat/phase5-production-publish`, added an explicit production-root R2 apply mode that requires an empty prefix, rejects incomplete candidates before remote access, and excludes `_staging/` from hydration, mutation, and verification. Production package payloads, APT by-hash objects, and checksum-named RPM metadata receive immutable one-year cache metadata; mutable activation, state, key, and configuration objects remain `no-store`. The protected publisher is confined to the initial `incus-gh-runner` `v1.0.0` release, uses the signing subkey, verifies the public fingerprint, repeats as a no-op, and runs clean Debian, Ubuntu, and Fedora installs. Staging-only recovery requires the exact `empty _staging only` phrase; production requires its own exact confirmation and separate protected job after staging succeeds.
Proof: Targeted R2/CLI tests, workflow syntax and policy, ShellCheck, the full repository `root:check`, and the live `root:phase5-source-proof` all pass. The live proof again verified GitHub release digests and pinned-workflow attestations before multi-architecture clean installs. Local Moon needed `PROTO_GO_VERSION=1.26.4` because its system resolver selected a newer globally installed Proto Go despite the mise pin; no repository pin changed.
Credential boundary: The `Meigma` vault contains the CI signing subkey attachment and passphrase plus the prefix-scoped staging R2 credential. No production R2 credential exists, and staging credentials will not be reused at the bucket root. Secret transfer and protected environment provisioning remain after code review; no secret values were printed or committed.
Next: Commit and open the production publication PR, obtain review, then run the protected `_staging/` empty-and-rebuild rehearsal from the exact PR head. Provision a separate production R2 credential and main-only GitHub environment before the first root publication after merge.

## 2026-07-21 20:22 — Protected staging recovery rehearsal passed
Delivery: Committed `feat(publish): add protected production publication` as `64dd763`, pushed `feat/phase5-production-publish`, and opened `meigma/packages` PR #11. Exact-head CI and Kusari Inspector are green.
Hosted proof: Workflow run #29888006129 passed the unprivileged real-release proof, then the protected staging job emptied only `_staging/`, rebuilt it from verified `incus-gh-runner` `v1.0.0` GitHub Release assets, applied 27 ordered R2 actions, rehydrated and verified the result, proved the repeat publish was a zero-action no-op, and completed clean Debian, Ubuntu, and Fedora installs. Production was explicitly disabled and skipped. The public staging manifest now selects only `v1.0.0` and contains four packages.
Environment handling: The first staging attempt was rejected before runner allocation because the environment allowed only `main` and the prior Phase 4 feature branch. Added a temporary exact-branch deployment policy for `feat/phase5-production-publish`, reran only the failed staging job successfully, then deleted that temporary policy. No failed attempt received secrets or touched R2.
Next: Review PR #11. After approval and merge, provision the main-only production environment with the Meigma-vault CI signing subkey/passphrase and a new separate bucket-root R2 credential, then perform the exact-confirmation production publication and public smoke checks.

## 2026-07-21 21:13 — Production repository published
Merge: After maintainer approval, verified PR #11 was unchanged at reviewed head `64dd763`, mergeable, and green in CI and Kusari Inspector. Squash-merged it on GitHub as `42b5fff`, fast-forwarded clean local `main`, and removed the integrated feature worktree plus local and remote branch. The initial `gh pr merge --delete-branch` reported only that `main` was already checked out in the correct separate Worktrunk worktree; GitHub had already merged successfully. Post-merge CI run #29889984694 passed on exact merge commit `42b5fff`.
Provisioning: Created the GitHub `production` environment with a custom deployment branch policy allowing only `main`. Set the public URL, R2 account/bucket/endpoint, and GPG fingerprints. Transferred the signing-only subkey, passphrase, and new production R2 access/secret keys directly from the 1Password `Meigma` vault into GitHub environment secrets without printing values. The production credential has no session token; the existing staging credential was not reused.
Publication: Before dispatch, the production manifest returned 404 while staging selected `v1.0.0`. Protected workflow run #29890108441 on exact merged commit `42b5fff` passed unprivileged source verification, staging revalidation, and first production-root publication. Production applied 27 ordered R2 actions for desired-state digest `e5aa10951f82578cde929e035fb39129e410274ae509752f117c38155b60f33e`, rehydrated and verified the result, proved the repeat apply was a no-op, and completed clean Debian 13, Ubuntu 26.04, and Fedora 44 installs. Root publication preserved `_staging/`.
Public proof: Production and staging manifests both select `v1.0.0` with four packages. `meigma.asc` has primary fingerprint `9C74476A669465EEB8D46AD8B0E68773B6E259F6`; APT `InRelease` and RPM `repomd.xml` return 200 with `no-store`; production DEB and RPM payloads return 200 with `public, max-age=31536000, immutable`. The prior technical note naming the `Homelab` vault is stale; all Meigma Packages signing and R2 items used here are in the `Meigma` vault.
Next: The first canonical production APT/RPM repository is live. The next small Phase 5 slice is consumer installation/dispatch and copy-paste public installation documentation; credential renewal automation and generalizing beyond the initial pinned `v1.0.0` selector remain follow-ups.

## 2026-07-21 21:25 — Trusted dispatch listener and install guide ready
Implementation: Opened `meigma/packages` PR #12 from `feat/phase5-consumer-dispatch` at `f316733`. The publish workflow now accepts only the `publish-package` repository dispatch type, maps only `project` and `tag` from its payload, validates them without secrets, and forces a valid request through protected staging and production. Dispatch cannot select staging deletion, skip a protected publication job, or supply confirmation phrases. Executable workflow policy requires this exact boundary and passed positive and negative mutation cases.
Documentation: Added public copy-paste APT and RPM installation instructions, full signing-key fingerprint verification, and repository removal commands. The live key reports `9C74476A669465EEB8D46AD8B0E68773B6E259F6`; the published RPM repository definition points to the canonical production base URL and enables signed repository-metadata verification.
Proof: Actionlint, workflow/ShellCheck policy, direct Go formatting/lint/build/tests, strict documentation build, and live public URL checks passed locally. The aggregate local Moon gate encountered its known system-toolchain resolver mismatch between mise Go 1.26.4 and global Proto Go 1.26.5, so the same individual gates were run directly; clean hosted CI run #29890965628 and Kusari Inspector both passed on exact PR head `f316733`.
Next: Review and merge PR #12. Once the listener exists on the default branch, add the pinned GitHub App token and release-published/manual dispatch workflow to `meigma/incus-gh-runner`, transfer its repo-scoped App client ID/private key from the 1Password `Meigma` vault, and perform the end-to-end no-op publication proof for `v1.0.0`.

## 2026-07-21 21:39 — Consumer package dispatch PR ready
Listener merge: After maintainer approval, verified `meigma/packages` PR #12 at reviewed head `f316733`, with CI and Kusari Inspector green, and squash-merged it as `f05cfdb`. The local `main` is fast-forwarded and clean, the feature worktree plus local/remote branch are removed, and post-merge CI run #29891359741 passed on the exact merge commit. The trusted `publish-package` listener now exists on the default branch.
Consumer implementation: Opened `meigma/incus-gh-runner` PR #47 from `feat/package-dispatch` at `50daeeb`. The new separate workflow reacts to stable published releases and supports a manual stable-tag proof, performs no checkout, has empty ambient token permissions, mints a short-lived `meigma-packaging-app` token restricted to `meigma/packages` with Contents write, and sends only the fixed `incus-gh-runner` project plus validated `vX.Y.Z` tag. The README and production deployment how-to now prefer the signed APT/RPM repository, verify the full signing-key fingerprint, provide copy-paste Debian/Ubuntu/Fedora setup, and retain release assets for direct or offline installation.
Provisioning and proof: Streamed the App client ID and private-key attachment from the 1Password `Meigma` vault directly into the repository-scoped `MEIGMA_PACKAGES_APP_CLIENT_ID` variable and `MEIGMA_PACKAGES_APP_PRIVATE_KEY` Actions secret without printing values. Actionlint and the full local `root:check` passed. Hosted CI, both CodeQL analyses, GitHub Pages, and Kusari Inspector passed on exact PR head `50daeeb`; release-dry-run jobs correctly skipped for this non-release PR.
Next: Review and merge `incus-gh-runner` PR #47. After the workflow exists on `master`, manually dispatch `v1.0.0`, verify that the App installation can reach only `meigma/packages`, and follow the resulting protected packages run through source validation, staging, production no-op, and clean client installs.

## 2026-07-21 22:05 — Consumer dispatch proven end to end
Merge: After maintainer approval, verified `meigma/incus-gh-runner` PR #47 remained at reviewed head `50daeeb` with all checks green, then squash-merged it as `dfd1e299`. Local `master` is fast-forwarded and clean; the integrated feature worktree plus local and remote branch were removed. Post-merge CI, code quality, Pages, Release Please, and security checks passed on the exact merge commit.
Fail-closed rehearsal: The first manual `v1.0.0` consumer run #29892115976 failed in four seconds while creating the App token because `meigma/packages` was not yet included in the App installation's selected repositories. It sent no dispatch and made no packages or R2 mutation. After GitHub sudo-mode authentication, the installation page confirmed exactly three selected repositories: `homebrew-tap`, `scoop-bucket`, and `packages`.
End-to-end proof: Fresh consumer run #29892388968 passed on `dfd1e299`, minted the repository-restricted App token, and dispatched the fixed `incus-gh-runner` / `v1.0.0` payload. It created `meigma/packages` repository-dispatch run #29892394354 on exact `main` commit `f05cfdb`, with both actor and triggering actor recorded as `meigma-packaging-app[bot]`. The downstream run passed secret-free source validation, protected staging, and protected production. Both environments reconciled seven ordered R2 actions, verified the result, proved the repeat reconciliation was a no-op, and completed clean Debian, Ubuntu, and Fedora installs.
Public proof: Production and staging manifests still select only `v1.0.0` and contain the four amd64/arm64 DEB/RPM packages. Production desired-state digest is `e5aa10951f82578cde929e035fb39129e410274ae509752f117c38155b60f33e`; staging is `b46cdc3f0d011a4522261f52b8559b02aceac130b3b333b8bb101387ad953e5d`. The public `meigma.asc` primary fingerprint remains `9C74476A669465EEB8D46AD8B0E68773B6E259F6`, and the mutable production manifest is served with `no-store`.
Next: Phase 5's first-release consumer-to-repository path is complete. Follow-ups can generalize publication beyond the initial pinned `v1.0.0`, automate credential renewal, and add rollback/operator automation as separate incremental slices.
